AStages.customizeStage('stage')
    .setAddTitle('Stage unlocked!') // Default
    .setAddSubTitle('with add subtitle') // Example
    .setRemoveTitle('Stage removed!') // Example
    .setRemoveSubTitle('with remove subtitle') // Example
    .setAddChatMessage('Custom chat message for add operation') // Example
    .setRemoveChatMessage('Custom chat message for remove operation') // Example
    .setFadeIn(20) // Default
    .setFadeOut(20) // Default
    .setStay(60) // Default